# venv
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 数据缓存（大文件不进 git；保留 .gitkeep 以让空目录入库）
data_cache/*
!data_cache/.gitkeep

# outputs 中间产物（图、临时 csv 等）；保留 .gitkeep
outputs/*
!outputs/.gitkeep

# 系统 & IDE
.DS_Store
.vscode/
.idea/

# 密钥（tushare token 等）
.env
*.token
config.local.*

# 做测试的临时 Notebook 文件
notebooks/test_*.ipynb

# 个人学习路线 / 持续教育任务（本地维护，不进 git）
LEARNING_TASKS.md

.envrc

# 个人 task / handoff 备忘, 不入仓
TASKS.md
